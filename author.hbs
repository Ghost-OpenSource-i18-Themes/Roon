{{!< default}}

<header role="banner">
    <div class="wrap">
        <h1><a href="{{@site.url}}">{{t "Author:"}} {{primary_author.name}}</a></h1>
        <hr>
    </div>
</header>

{{> "loop"}}

{{> "footer"}}